<h1 align= "center"> All Users</h1>
<div align= "center">
	<% @users.each do |user| %>
		<ul class= "listing">
			<div class= "row">
				<div class= "well col-md-4 col-md-offset-4">
					<li><%= link_to image_tag(user.image.url(:thumb))%></li>
					<li> <%= link_to user.first_name, user %></li>
				</div>
			</div>
		</ul>
	<% end %>
</div>